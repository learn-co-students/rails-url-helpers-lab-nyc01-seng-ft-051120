<div>
  <% @students.each do |student| %>
    <div><%= link_to student.first_name + " " + student.last_name, student_path(student.id) %></div>
    div><%= link_to student.first_name + " " + student.last_name, "students/#{student.id}/active" %></div>
    <%student.active=true%>

  <% end %>
</div>
